# Babel Tower Phase 2：多格式核心契约

日期：2026-08-17

## 阶段结论

Phase 2 已将“格式扩展不重建核心”落实为可执行契约：模拟适配器能够流式导入、分页恢复、重新抽取、显式绑定并按冻结快照恢复导出。此阶段证明公共核心可以承载不同格式，不代表 TXT、Markdown、EPUB 或 OCR 已达到产品支持等级；这些真实格式仍按 Phase 3 以后逐个纵向验收。

阶段代码审查复审为 `APPROVE`；首次架构审查提出的“候选集合外绑定”和“未完整代可激活”两个阻塞项，已由 canonical 候选校验及 `Building → Validated → Active` 状态机关闭。

Windows 继续遵守既定发布边界：Linux 只负责生成 Windows 安装包，不把交叉构建或 Wine 结果称为 Windows 实机验证。

## 做了什么，以及为什么

### 1. 资源图和稳定定位器

- `babel-resource-graph` 定义版本化资源节点、关系和字节、结构、文字、空间、时间、帧区间定位器。
- 校验器拒绝重复身份、悬空边、包含/阅读顺序环、非法范围和错误的图片区域归属。
- 资源类型已预留图片、字体、样式、附件、音频、视频、字幕和时间线，但没有提前实现相应格式逻辑。

公共编辑模型因此只理解资源和定位器，不需要知道 EPUB ZIP 成员、Markdown AST 或未来游戏引擎的私有表结构。

### 2. TIR 结构保护

- `babel-tir` 将可翻译文本、受保护标签、占位符、换行和资源引用分离。
- 源内容和译文覆盖都必须通过 schema、嵌套、保护序列和占位符规则校验。
- 同名占位符不能声明冲突规则；新增占位符或违反数量规则会失败关闭。

这让人工译文可以自由修改文本，同时不能静默破坏格式必须保留的结构。

### 3. 导入代与绑定账本

- SQLite schema v6 新增导入代、批次 receipt、资源、关系、抽取单元、绑定记录和人工绑定决策。
- 导入先写 `Building` 代；分页批次用 `batch_id + payload_hash` 幂等重放；资源、抽取单元和绑定校验完成后显式封口为 `Validated`，只有该状态才能在单事务中替换活动代。
- 重新抽取明确区分 `Exact / Shifted / Ambiguous / Orphaned`。只有 `Exact` 可以自动继承；其他类型必须保留候选并等待显式决策。
- 重复记录和重复决策不会覆盖既有事实；候选集合变化会使旧决策失败。

旧活动代因此在新导入失败或中断时仍可打开，译文不会依赖当前序号猜测绑定。

### 4. 流式适配器协议与能力句柄

- `babel-adapter-protocol` 定义版本协商、清单、安全上限、分页游标、取消/单调时钟截止时间、资源清点、TIR 抽取、导出计划、物化和验证；页预算、累计输出预算和零预算都执行失败关闭。
- 适配器只收到核心签发的对象/暂存能力句柄，不能提交任意文件路径；伪造句柄会被拒绝。
- Protobuf envelope 复用有界本地 IPC frame；既有运行时测试覆盖本地 socket、握手 nonce、能力 token、版本和帧大小。
- `babel-adapter-host` 用持久 DAG lease/fencing 执行确定性产物，Ready 结果可重用，过期 owner 不能发布。

首个 `MockTextAdapter` 是契约夹具，不是产品 TXT 适配器。它故意保持简单，用于证明所有格式必须遵守同一条边界。

### 5. 流式 CAS、writer 公平性和恢复导出

- CAS 从 `Read` 分块写入、同步并计算 SHA-256，不再要求完整源文件进入内存。
- recovery 哈希校验改为流式读取。
- writer 在最多 32 个连续交互命令后，如果后台工作已就绪，会执行一个后台命令；交互仍保持默认优先。
- 导出计划冻结提交序号、全局覆盖哈希、单元顺序和逐单元哈希。物化使用有界页和可恢复游标，适配器重启后可继续；活动译文改变不能混入旧快照。

## 有限的第二轮优化

闭环通过后只处理了一个有明确复杂度证据的热区：原物化路径每一页都重新哈希并建立全部译文映射，工作量随 `单元数 × 页数` 增长。现在计划生成时冻结逐单元哈希，物化只检查当前页，整体检查工作量随单元数线性增长。

没有在本阶段引入零拷贝 IPC、自定义 allocator、并行 SQLite writer、复杂缓存或格式专用快速路径。这些都必须等真实 TXT/Markdown/EPUB 语料和 profile 证明后再决定。

## 模块边界

```text
babel-domain
    ↑
babel-resource-graph    babel-tir
          ↑                ↑
          └── babel-adapter-protocol
                       ↑
babel-runtime ── babel-adapter-host

babel-storage
    ↑
babel-application  # 只在集成测试中组合模拟适配器合同
```

CI 依赖门禁禁止资源图/TIR 反向依赖存储或宿主、协议依赖运行时/存储，以及宿主直接依赖权威存储或应用层。

## 验证命令

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
./tools/check-architecture.sh
```

端到端 `g2_contract` 额外验证：CAS 发布、能力授权、分页清点、资源图验证、分页 TIR 抽取、批次重放、代际切换、源内容重排后的精确映射、中断后恢复导出、输出验证和冻结后编辑拒绝。

当前本地结果：81 项测试通过，Clippy 零警告，格式检查、依赖方向检查和 diff 检查通过。Phase 0 包闭包探针仍为 `SUPPORTED`；Windows 保持 `BUILT_UNVERIFIED`，生产整包保持 `FALSIFIED`，不借 Phase 2 核心完成度提前提升发布声明。

## 明确未完成

- 真实 TXT 产品闭环、编辑命令与导出发布仍属于 Phase 3。
- Markdown、EPUB 和图片/OCR 适配器尚未实现。
- 当前宿主证明能力与 IPC 边界，但尚未完成生产级 worker 进程生命周期、OS 沙箱和资源配额执行器。
- 绑定夹具覆盖精确匹配与歧义决策不变量，固定 B 语料的拆分/合并/近似匹配指标留给真实格式阶段。
- 没有运行 Windows 本机测试；Linux 不能替代 Windows 锁、命名管道、安装和故障恢复证据。
- 真正断电、磁盘写满、跨文件系统导出和大语料 RSS/吞吐预算仍按后续阶段门禁执行。

## 用户审查清单

- [ ] 是否接受资源图/TIR 是所有格式的公共边界，格式私有往返数据不得进入编辑公共模型。
- [ ] 是否接受只有 `Exact` 自动继承；`Shifted / Ambiguous / Orphaned` 默认不自动套用旧译文。
- [ ] 是否接受新导入代必须完整构建后原子激活，失败时继续使用旧活动代。
- [ ] 是否接受适配器只能通过能力句柄访问核心授权对象和暂存区。
- [ ] 是否接受导出冻结后继续编辑不会改变本次导出，而是在下一次导出体现。
- [ ] 是否接受本轮第二次优化仅消除已证明的分页重复扫描，其余性能工作等待真实格式 profile。
- [ ] 是否接受 Phase 2 的模拟适配器仅证明核心契约，不构成 TXT 产品支持声明。
- [ ] 是否要求在 Phase 3 前先补生产级外部 worker 沙箱，还是与 TXT 纵向闭环同步完成。
