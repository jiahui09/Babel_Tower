# OCR 扩展架构

Babel Tower 不把 OCR 视为一个固定模型，而把它视为可替换的能力提供者。翻译核心只接收 `babel-ocr` 定义的统一结果，不依赖 PaddleOCR、Tesseract 或未来的 AI 引擎。

## 稳定边界

- `OcrProfile`：语言、方向、版面类型、置信度阈值和坐标保留策略。
- `OcrRequest`：输入类型、媒体类型、源文件 SHA-256 和图片字节。
- `OcrEngine`：引擎描述、能力声明和识别入口。
- `OcrDocument`：页面、区域、阅读顺序、语言、置信度、原文和归一化文本。

识别结果必须带有源哈希、引擎版本、运行库版本、模型 ID 和 Profile。OCR 候选不能直接写入译文修订或覆盖原始资源。

## 引擎路由

| 场景 | 首选能力 | 当前状态 |
| --- | --- | --- |
| 普通图片 | 检测 + 识别 + 坐标 | PP-OCRv6 tiny 已实现 |
| PDF 已有文字层 | 文字层解析 | 后续实现 |
| PDF 扫描页 | 文档版面 + OCR | 后续实现 |
| 中英日韩混排 | 区域级语言路由 | Profile 已预留，后续模型接入 |
| 竖排文字 | 方向识别 + 专用识别 | Profile/能力声明已预留 |
| 表格 | 表格检测 + 单元格识别 | `OcrLayoutMode::Table` 已预留 |
| AI 辅助 OCR | 独立引擎适配器 + 人工确认 | 接口已预留，首版不启用 |

新增引擎时只需实现 `OcrEngine`、声明能力并加入资产清单；翻译、搜索、人工修正、嵌字和导出不应增加引擎分支。

## 当前 PP-OCR 实现边界

当前 worker 声明支持图片、PDF 页面和文档页面输入，但实际模型能力仍是普通图片文字识别：不声明语言自动检测、版面分析、表格识别或竖排识别。这样可以让调用方根据能力声明决定是否降级或请求人工复核，而不会误把普通 OCR 当成完整 PDF OCR。
